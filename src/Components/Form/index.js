import React from 'react'

const Form = () => {
  return (
    <div>
        <Form>
            <input placeholder="Add Todo Item"></input>
            <button>Add</button>
        </Form>

    </div>
  )
}

export default Form